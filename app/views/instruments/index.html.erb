<div class="container">
  <%= render "shared/form_search_bar" %>

  <div class="main-container">
    <div class= "list-index col-7">
      <%= turbo_frame_tag "instruments" do %>

  <div class="row">
    <div class= "col-lg-6">
      <h1>All instruments</h1>
    </div>
    <div class= "share">
      <div class= "col-lg-6">
        <%= link_to "Share your instrument",new_instrument_path %>
      </div>
    </div>
  </div>
 
      <%= render @instruments %>
      <% end %>
    </div>
      <div style="height: 100vh;" class="map_index col-5"
      data-controller="map"
      data-map-markers-value="<%= @markers.to_json %>"
      data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">
      </div>

    <div style="width: 100%; height: 600px;" class="mt-3"
  data-controller="map"
  data-map-markers-value="<%= @markers.to_json %>"
  data-map-api-key-value="<%= ENV['MAPBOX_API_KEY'] %>">

  </div>

  <%= render "pager", pagy: @pagy %>
</div>
